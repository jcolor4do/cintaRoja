// 16.- Escribe una función que imprima todos
//      los números naturales en reversa, 
//      desde n hasta 1.

// let valorMayor=10;
function deReversa(valorMayor) {
    for (let index = valorMayor; index >=1; index--) {
        console.log(index);
    }
}

deReversa(120);